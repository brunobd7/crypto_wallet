<h1><%= I18n.t('editing.coin') %></h1>

<%= render 'form', coin: @coin %>

<%= link_to I18n.t('links.show'), @coin , class:"btn btn-primary" %>
<%= link_to I18n.t('links.back'), coins_path , class:"btn btn-primary"%>
